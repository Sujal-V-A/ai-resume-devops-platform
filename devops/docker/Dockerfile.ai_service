FROM python:3.11-slim
WORKDIR /app
COPY ai_service/requirements.txt* ./
# In case requirements.txt doesn't exist, we install direct modules
RUN pip install --no-cache-dir flask flask-cors PyPDF2 werkzeug gunicorn
COPY ai_service/ ./
EXPOSE 5050
CMD ["gunicorn", "--bind", "0.0.0.0:5050", "app:app"]
